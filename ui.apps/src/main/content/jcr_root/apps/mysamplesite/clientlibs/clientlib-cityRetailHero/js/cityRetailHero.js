(function () {
    "use strict";

    // Initialize the component
    function init() {
        // Add any JavaScript functionality here
        // For example, animation on scroll or interactive features
    }

    // When DOM is ready
    if (document.readyState !== "loading") {
        init();
    } else {
        document.addEventListener("DOMContentLoaded", init);
    }
})(); 